# Push for new release

```bash
git tag -d {TAG NAME}
git push origin :refs/tags/{TAG NAME}
git tag {TAG NAME}
git push origin {TAG NAME}
```

```bash
git tag {TAG NAME}
git push origin {TAG NAME}
```
